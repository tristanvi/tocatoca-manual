---
import { LinkButton } from '@astrojs/starlight/components';

const { data } = Astro.locals.starlightRoute.entry;
const { title = data.title, tagline, actions = [] } = data.hero || {};
const PAGE_TITLE_ID = '_top';
---

<section class="toc-hero" aria-labelledby={PAGE_TITLE_ID}>
	<div class="toc-hero__copy">
		<h1 id={PAGE_TITLE_ID} data-page-title set:html={title} />
		{tagline && <div class="toc-hero__tagline" set:html={tagline} />}
		{
			actions.length > 0 && (
				<div class="toc-hero__actions">
					{actions.map(
						({ attrs: { class: className, ...attrs } = {}, icon, link: href, text, variant }) => (
							<LinkButton {href} {variant} icon={icon?.name} class:list={[className]} {...attrs}>
								{text}
								{icon?.html && <Fragment set:html={icon.html} />}
							</LinkButton>
						)
					)}
				</div>
			)
		}
	</div>
	<div class="toc-hero__figure" aria-hidden="true">
		<img src="/images/brand/Vixelle-main.png" alt="" width="500" height="500" loading="eager" />
	</div>
</section>
